# rpp - Rust PE Parser

## Overview
This project is a PE parser library written for the purpose of learning Rust.

## Usage
A sample program is provided in `dump.rs`.  
Running `cargo run` will parse `kernel32.dll` and print the parsed results.

## License
MIT

## Planned Features
- ARM64 exception directory parsing
- Resource directory parsing
- LoadConfig directory parsing
